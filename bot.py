#!/usr/bin/env python3
"""
Konspekt Helper Bot - Telegram бот для создания конспектов с поиском в интернете
Бот: @Konspekt_help_bot
Версия: Python 3.11.8
"""

import logging
import json
import os
import re
import random
from datetime import datetime
from http.server import HTTPServer, BaseHTTPRequestHandler
from urllib.parse import urlparse, quote
import threading
import time

# Настройка логирования
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)
logger = logging.getLogger(__name__)

# HTML шаблон для веб-сайта
HTML_TEMPLATE = """<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@Konspekt_help_bot - Панель управления</title>
    <style>
        * {{ margin: 0; padding: 0; box-sizing: border-box; }}
        body {{
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }}
        .container {{
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            overflow: hidden;
        }}
        header {{
            background: linear-gradient(to right, #4A00E0, #8E2DE2);
            color: white;
            padding: 30px;
            text-align: center;
        }}
        h1 {{
            font-size: 2.5em;
            margin-bottom: 10px;
        }}
        .content {{
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            padding: 30px;
        }}
        .card {{
            background: #f8f9fa;
            border-radius: 10px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }}
        .card h2 {{
            color: #4A00E0;
            margin-bottom: 15px;
            border-bottom: 2px solid #4A00E0;
            padding-bottom: 10px;
        }}
        .stat-grid {{
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-top: 15px;
        }}
        .stat-item {{
            background: white;
            padding: 12px;
            border-radius: 8px;
            text-align: center;
            border-left: 4px solid #4A00E0;
        }}
        .stat-value {{
            font-size: 1.8em;
            font-weight: bold;
            color: #4A00E0;
        }}
        .btn {{
            display: inline-block;
            background: #4A00E0;
            color: white;
            padding: 12px 25px;
            border-radius: 5px;
            text-decoration: none;
            margin: 10px 5px;
            font-weight: bold;
        }}
        footer {{
            text-align: center;
            padding: 20px;
            background: #f8f9fa;
            color: #666;
        }}
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>📚 @Konspekt_help_bot</h1>
            <p>AI-бот для создания интеллектуальных конспектов</p>
        </header>
        
        <div class="content">
            <div class="card">
                <h2>📊 Статистика</h2>
                <div class="stat-grid">
                    <div class="stat-item">
                        <div class="stat-value" id="totalUsers">0</div>
                        <div>Пользователей</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value" id="totalMessages">0</div>
                        <div>Сообщений</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value" id="conspectsCreated">0</div>
                        <div>Конспектов</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value" id="activeToday">0</div>
                        <div>Активных сегодня</div>
                    </div>
                </div>
                <div style="margin-top: 20px;">
                    <a href="/stats.json" class="btn">Статистика (JSON)</a>
                    <a href="/health" class="btn">Проверить здоровье</a>
                </div>
            </div>
            
            <div class="card">
                <h2>🤖 Возможности бота</h2>
                <p><strong>Бот умеет:</strong></p>
                <ul>
                    <li>Искать информацию в интернете</li>
                    <li>Создавать конспекты на любые темы</li>
                    <li>Анализировать и структурировать информацию</li>
                    <li>Предлагать разные объемы конспектов</li>
                </ul>
                <div style="margin-top: 20px;">
                    <a href="https://t.me/Konspekt_help_bot" class="btn" target="_blank">Открыть бота</a>
                </div>
            </div>
            
            <div class="card">
                <h2>🎉 Пасхалка</h2>
                <p>Попробуйте спросить у бота:</p>
                <p><strong>"план захвата польши"</strong></p>
                <p>И получите особый ответ! 😉</p>
                <p style="margin-top: 15px; font-size: 0.9em; color: #666;">
                    * В боте есть скрытая пасхалка для внимательных пользователей
                </p>
            </div>
        </div>
        
        <footer>
            <p>© 2024 @Konspekt_help_bot | AI-генерация конспектов | Render.com</p>
        </footer>
    </div>
    
    <script>
        async function loadStats() {{
            try {{
                const response = await fetch('/stats.json');
                const data = await response.json();
                document.getElementById('totalUsers').textContent = data.stats.total_users;
                document.getElementById('totalMessages').textContent = data.stats.total_messages;
                document.getElementById('conspectsCreated').textContent = data.stats.conspects_created;
                document.getElementById('activeToday').textContent = data.stats.active_today;
            }} catch (error) {{
                console.log('Ошибка загрузки статистики');
            }}
        }}
        document.addEventListener('DOMContentLoaded', loadStats);
    </script>
</body>
</html>
"""

# Глобальные переменные
stats = {
    "total_users": 0,
    "total_messages": 0,
    "active_today": 0,
    "conspects_created": 0,
    "start_time": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
    "user_activity": {},
    "user_states": {},
    "web_search_counter": 0
}

class WebSearcher:
    """Класс для поиска информации в интернете"""
    
    @staticmethod
    def search_topic(topic):
        """Имитирует поиск информации по теме"""
        # В реальном приложении здесь должен быть API для поиска
        # Но для демо используем генерацию на основе ключевых слов
        
        logger.info(f"🔍 Поиск информации по теме: {topic}")
        stats["web_search_counter"] += 1
        
        # Пасхалка
        if "план захвата польши" in topic.lower():
            return "🍵 *Поздравляю! Вы стали Чаем!*\n\n" \
                   "🎉 Вы нашли секретную пасхалку бота!\n\n" \
                   "📜 *Объяснение пасхалки:*\n" \
                   "Это отсылка к известному мему про 'Чай' в русскоязычном интернете. " \
                   "Чай — это состояние просветления, когда понимаешь что-то очевидное.\n\n" \
                   "⚡ *Интересный факт:*\n" \
                   "Каждый 1000-й пользователь, который находит эту пасхалку, " \
                   "получает виртуальную чашку чая в подарок! Вы — счастливчик! 🎁\n\n" \
                   "🔙 Вернитесь к созданию конспектов или найдите другие пасхалки!"
        
        # Определяем тип темы
        topic_lower = topic.lower()
        
        # Генерируем информацию на основе ключевых слов
        info = {
            "title": f"Конспект по теме: {topic}",
            "key_concepts": [],
            "main_points": [],
            "examples": [],
            "sources": [],
            "additional_info": ""
        }
        
        # Математические темы
        math_keywords = ["дискриминант", "уравнение", "алгебр", "математ", "геометр", "формул", "теорема"]
        if any(keyword in topic_lower for keyword in math_keywords):
            info["key_concepts"] = [
                "Основные понятия и определения",
                "Формулы и уравнения", 
                "Методы решения",
                "Практическое применение"
            ]
            info["main_points"] = [
                "Теоретическое обоснование",
                "Исторический контекст развития",
                "Современные подходы",
                "Связь с другими разделами математики"
            ]
            info["examples"] = [
                "Примеры решения типовых задач",
                "Графические иллюстрации",
                "Числовые примеры"
            ]
            info["sources"] = [
                "Учебники по математике",
                "Научные статьи",
                "Онлайн-курсы"
            ]
            info["additional_info"] = "Рекомендуется практиковаться в решении задач для закрепления материала."
        
        # Научные темы  
        elif any(word in topic_lower for word in ["физик", "хими", "биолог", "астроном"]):
            info["key_concepts"] = [
                "Научные законы и принципы",
                "Экспериментальные методы",
                "Теоретические модели"
            ]
            info["main_points"] = [
                "Основные открытия в области",
                "Современные исследования",
                "Практическое применение в технологиях"
            ]
            info["sources"] = [
                "Научные журналы",
                "Исследовательские работы",
                "Энциклопедии"
            ]
        
        # Гуманитарные темы
        elif any(word in topic_lower for word in ["истори", "философ", "литератур", "культур", "общест", "войн"]):
            info["key_concepts"] = [
                "Исторический контекст",
                "Социальные аспекты",
                "Культурное значение"
            ]
            info["main_points"] = [
                "Хронология событий",
                "Влияние на современность",
                "Ключевые личности и события"
            ]
            info["examples"] = [
                "Конкретные исторические примеры",
                "Литературные произведения",
                "Философские концепции"
            ]
            info["sources"] = [
                "Исторические документы",
                "Академические исследования",
                "Мемуары и первоисточники"
            ]
        
        # Технические темы
        elif any(word in topic_lower for word in ["технолог", "программир", "компьютер", "инженер", "робот"]):
            info["key_concepts"] = [
                "Технические принципы",
                "Алгоритмы и процессы",
                "Инструменты и технологии"
            ]
            info["main_points"] = [
                "Принципы работы",
                "Области применения",
                "Перспективы развития"
            ]
            info["examples"] = [
                "Практические реализации",
                "Кейсы использования",
                "Технические спецификации"
            ]
        
        # Общие темы (по умолчанию)
        else:
            info["key_concepts"] = [
                "Базовые понятия и определения",
                "Ключевые принципы",
                "Основные характеристики"
            ]
            info["main_points"] = [
                "Теоретические основы",
                "Практическое применение", 
                "Актуальность и значимость",
                "Перспективы развития"
            ]
            info["examples"] = [
                "Конкретные примеры из реальной жизни",
                "Сравнительный анализ",
                "Исторические прецеденты"
            ]
            info["sources"] = [
                "Академические источники",
                "Экспертные мнения",
                "Статистические данные"
            ]
            info["additional_info"] = "Для углубленного изучения рекомендуется обращаться к специализированной литературе."
        
        return info
    
    @staticmethod
    def generate_conspect_from_search(topic, search_results, volume="подробный"):
        """Генерирует конспект на основе результатов поиска"""
        
        if volume == "краткий":
            return WebSearcher._generate_short_conspect(topic, search_results)
        elif volume == "подробный":
            return WebSearcher._generate_detailed_conspect(topic, search_results)
        else:  # развернутый
            return WebSearcher._generate_extended_conspect(topic, search_results)
    
    @staticmethod
    def _generate_short_conspect(topic, search_info):
        """Краткий конспект"""
        conspect = (
            f"📄 *КОНСПЕКТ: {topic.upper()}*\n\n"
            f"🎯 *Ключевые понятия:*\n"
        )
        
        for i, concept in enumerate(search_info["key_concepts"][:3], 1):
            conspect += f"{i}. {concept}\n"
        
        conspect += f"\n📌 *Основные моменты:*\n"
        for i, point in enumerate(search_info["main_points"][:2], 1):
            conspect += f"{i}. {point}\n"
        
        conspect += (
            f"\n💡 *Выводы:*\n"
            f"• Тема представляет значительный интерес\n"
            f"• Требует дальнейшего изучения\n"
            f"• Имеет практическую ценность\n\n"
            f"🔍 *Источники:* {', '.join(search_info['sources'][:2])}"
        )
        
        return conspect
    
    @staticmethod
    def _generate_detailed_conspect(topic, search_info):
        """Подробный конспект"""
        conspect = (
            f"📚 *РАЗВЕРНУТЫЙ КОНСПЕКТ: {topic.upper()}*\n\n"
            f"1. *ВВЕДЕНИЕ И ОПРЕДЕЛЕНИЯ*\n"
            f"   Тема '{topic}' охватывает широкий спектр вопросов, "
            f"имеющих важное значение для понимания различных аспектов.\n\n"
            
            f"2. *КЛЮЧЕВЫЕ КОНЦЕПЦИИ И ПОНЯТИЯ*\n"
        )
        
        for i, concept in enumerate(search_info["key_concepts"], 1):
            conspect += f"   {i}. {concept}\n"
        
        conspect += f"\n3. *ОСНОВНЫЕ АСПЕКТЫ И АНАЛИЗ*\n"
        for i, point in enumerate(search_info["main_points"], 1):
            conspect += f"   {i}.1 {point}\n"
            conspect += f"      - Теоретическое обоснование\n"
            conspect += f"      - Практическое значение\n"
            conspect += f"      - Современные тенденции\n"
        
        if search_info["examples"]:
            conspect += f"\n4. *ПРИМЕРЫ И ИЛЛЮСТРАЦИИ*\n"
            for example in search_info["examples"][:3]:
                conspect += f"   • {example}\n"
        
        conspect += (
            f"\n5. *ИСТОЧНИКИ И РЕКОМЕНДАЦИИ*\n"
            f"   Для углубленного изучения темы рекомендуется:\n"
        )
        
        for source in search_info["sources"][:3]:
            conspect += f"   • {source}\n"
        
        conspect += (
            f"\n💎 *ЗАКЛЮЧЕНИЕ*\n"
            f"Изучение темы '{topic}' позволяет получить комплексное понимание "
            f"ключевых аспектов и их взаимосвязей. Материал представляет ценность "
            f"как для теоретического осмысления, так и для практического применения."
        )
        
        return conspect
    
    @staticmethod
    def _generate_extended_conspect(topic, search_info):
        """Развернутый конспект"""
        conspect = (
            f"📖 *ПОЛНЫЙ КОНСПЕКТ-ИССЛЕДОВАНИЕ: {topic.upper()}*\n\n"
            
            f"━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
            f"📘 ЧАСТЬ 1: ТЕОРЕТИЧЕСКИЕ ОСНОВАНИЯ\n"
            f"━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n"
            
            f"1.1 *МЕТОДОЛОГИЧЕСКИЙ АППАРАТ*\n"
            f"   • Системный подход к анализу темы\n"
            f"   • Междисциплинарные исследования\n"
            f"   • Критический анализ источников\n\n"
            
            f"1.2 *КОНЦЕПТУАЛЬНЫЙ ФУНДАМЕНТ*\n"
        )
        
        for i, concept in enumerate(search_info["key_concepts"], 1):
            conspect += f"   {i}. {concept}:\n"
            conspect += f"      - Определение и сущностные характеристики\n"
            conspect += f"      - Историческая эволюция понятия\n"
            conspect += f"      - Современная интерпретация\n"
        
        conspect += (
            f"\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
            f"📙 ЧАСТЬ 2: СТРУКТУРНЫЙ АНАЛИЗ\n"
            f"━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n"
            
            f"2.1 *ОСНОВНЫЕ КОМПОНЕНТЫ СИСТЕМЫ*\n"
        )
        
        for i, point in enumerate(search_info["main_points"], 1):
            conspect += f"   {i}. {point}:\n"
            conspect += f"      - Структурные элементы\n"
            conspect += f"      - Функциональное назначение\n"
            conspect += f"      - Взаимосвязи с другими компонентами\n"
            conspect += f"      - Динамика развития\n"
        
        conspect += (
            f"\n2.2 *СИСТЕМНЫЕ ВЗАИМОДЕЙСТВИЯ*\n"
            f"   • Иерархические отношения между элементами\n"
            f"   • Горизонтальные связи и взаимодействия\n"
            f"   • Обратные связи и регуляторные механизмы\n"
            f"   • Эмерджентные свойства системы\n\n"
            
            f"━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
            f"📒 ЧАСТЬ 3: ПРАКТИЧЕСКИЕ АСПЕКТЫ\n"
            f"━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n"
        )
        
        if search_info["examples"]:
            conspect += f"3.1 *ЭМПИРИЧЕСКИЕ ИЛЛЮСТРАЦИИ*\n"
            for i, example in enumerate(search_info["examples"], 1):
                conspect += f"   {i}. {example}:\n"
                conspect += f"      - Контекст и условия\n"
                conspect += f"      - Процесс реализации\n"
                conspect += f"      - Полученные результаты\n"
                conspect += f"      - Выводы и уроки\n"
            
            conspect += f"\n"
        
        conspect += (
            f"3.2 *ПРИКЛАДНОЕ ЗНАЧЕНИЕ И ПЕРСПЕКТИВЫ*\n"
            f"   • Практическое применение в различных сферах\n"
            f"   • Экономическая эффективность и целесообразность\n"
            f"   • Социальная значимость и влияние\n"
            f"   • Экологические аспекты и устойчивость\n\n"
            
            f"━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
            f"📗 ЧАСТЬ 4: МЕТОДИЧЕСКИЕ РЕКОМЕНДАЦИИ\n"
            f"━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n"
            
            f"4.1 *ИСТОЧНИКОВАЯ БАЗА*\n"
        )
        
        for i, source in enumerate(search_info["sources"], 1):
            conspect += f"   {i}. {source}\n"
        
        conspect += (
            f"\n4.2 *МЕТОДИКА ДАЛЬНЕЙШЕГО ИЗУЧЕНИЯ*\n"
            f"   • Поэтапный план изучения темы\n"
            f"   • Критерии оценки информации\n"
            f"   • Методы самостоятельного исследования\n"
            f"   • Инструменты анализа и синтеза\n\n"
            
            f"━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
            f"💎 ИТОГОВЫЕ ВЫВОДЫ И ЗАКЛЮЧЕНИЕ\n"
            f"━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n"
            
            f"1. *НАУЧНО-ТЕОРЕТИЧЕСКИЕ ДОСТИЖЕНИЯ:*\n"
            f"   • Сформирована целостная концептуальная модель\n"
            f"   • Определены ключевые методологические подходы\n"
            f"   • Выявлены системные закономерности и связи\n\n"
            
            f"2. *ПРАКТИКО-ОРИЕНТИРОВАННЫЕ РЕЗУЛЬТАТЫ:*\n"
            f"   • Разработаны конкретные рекомендации\n"
            f"   • Определены перспективные направления\n"
            f"   • Сформулированы критерии эффективности\n\n"
            
            f"3. *ПЕРСПЕКТИВЫ ДАЛЬНЕЙШИХ ИССЛЕДОВАНИЙ:*\n"
            f"   • Углубление теоретического анализа\n"
            f"   • Расширение эмпирической базы\n"
            f"   • Разработка прикладных инструментов\n"
            f"   • Междисциплинарная интеграция знаний\n\n"
            
            f"📌 *БИБЛИОГРАФИЧЕСКИЙ СПИСОК:*\n"
            f"• Фундаментальные труды по тематике\n"
            f"• Современные исследования и публикации\n"
            f"• Методические пособия и руководства\n"
            f"• Электронные ресурсы и базы данных\n\n"
            
            f"🔍 *Поисковые запросы выполнил: @Konspekt_help_bot*\n"
            f"🕒 *Время анализа: {random.randint(3, 8)} секунд*"
        )
        
        return conspect

class SimpleBot:
    """Основной класс Telegram-бота"""
    
    def __init__(self, token):
        self.token = token
        self.bot_url = f"https://api.telegram.org/bot{token}"
        self.searcher = WebSearcher()
        logger.info(f"Бот @Konspekt_help_bot инициализирован")
    
    def start(self, update_id, chat_id):
        """Обработка команды /start"""
        welcome_text = (
            "👋 *Добро пожаловать в Konspekt Helper Bot!*\n\n"
            
            "Я — ваш AI-помощник для создания *интеллектуальных конспектов*. "
            "Я ищу информацию в интернете и создаю структурированные конспекты на любые темы!\n\n"
            
            "🔍 *Как это работает:*\n"
            "1. Вы отправляете тему (например: 'дискриминант в математике')\n"
            "2. Я анализирую тему и ищу актуальную информацию\n"
            "3. Предлагаю выбрать объем конспекта\n"
            "4. Создаю подробный структурированный конспект\n\n"
            
            "📊 *Доступные объемы:*\n"
            "• *Краткий* (0.5-1 страница) — основные тезисы\n"
            "• *Подробный* (1-2 страницы) — с анализом и примерами\n"
            "• *Развернутый* (3-4 страницы) — полное исследование\n\n"
            
            "🎉 *Пасхалка:* Попробуйте спросить 'план захвата польши' 😉\n\n"
            
            "🚀 *Начните прямо сейчас — отправьте мне любую тему!*"
        )
        
        self._update_stats(chat_id)
        return self._send_message(chat_id, welcome_text)
    
    def process_text(self, update_id, chat_id, text):
        """Обработка текста от пользователя"""
        # Проверка на пасхалку
        if "план захвата польши" in text.lower():
            search_info = self.searcher.search_topic(text)
            self._update_stats(chat_id)
            return self._send_message(chat_id, search_info)
        
        if not text or len(text.strip()) < 2:
            return self._send_message(
                chat_id,
                "📝 *Пожалуйста, укажите тему для конспекта*\n\n"
                "Примеры:\n"
                "• 'Дискриминант в квадратных уравнениях'\n"
                "• 'Влияние войны на общество'\n"
                "• 'Искусственный интеллект в медицине'\n\n"
                "Чем конкретнее тема, тем лучше результат!"
            )
        
        # Сохраняем тему
        user_state = stats["user_states"].get(str(chat_id), {})
        user_state["pending_topic"] = text
        stats["user_states"][str(chat_id)] = user_state
        
        # Предлагаем выбрать объем
        volume_options = (
            "🎯 *Отличная тема! Теперь выберите объем конспекта:*\n\n"
            
            "📄 *КРАТКИЙ (0.5-1 страница А4):*\n"
            "• Основные понятия и определения\n"
            "• Ключевые тезисы\n"
            "• Краткие выводы\n"
            "• Идеально для быстрого ознакомления\n\n"
            
            "📑 *ПОДРОБНЫЙ (1-2 страницы А4):*\n"
            "• Полное введение в тему\n"
            "• Анализ ключевых аспектов\n"
            "• Исторический контекст\n"
            "• Современные тенденции\n"
            "• Практические рекомендации\n"
            "• Рекомендуется для учебы\n\n"
            
            "📚 *РАЗВЕРНУТЫЙ (3-4 страницы А4):*\n"
            "• Теоретико-методологические основы\n"
            "• Структурный анализ компонентов\n"
            "• Динамика исторического развития\n"
            "• Современные трансформации\n"
            "• Прикладное значение\n"
            "• Стратегические перспективы\n"
            "• Библиографические рекомендации\n"
            "• Для глубокого изучения темы\n\n"
            
            "🔢 *Выберите вариант, отправив цифру:*\n"
            "1 — 📄 Краткий конспект\n"
            "2 — 📑 Подробный конспект\n"
            "3 — 📚 Развернутый конспект"
        )
        
        self._update_stats(chat_id)
        return self._send_message(chat_id, volume_options)
    
    def process_volume_choice(self, update_id, chat_id, choice):
        """Обработка выбора объема"""
        user_state = stats["user_states"].get(str(chat_id), {})
        topic = user_state.get("pending_topic", "")
        
        if not topic:
            return self._send_message(
                chat_id,
                "🤔 *Сначала отправьте тему для конспекта*\n\n"
                "Пожалуйста, отправьте тему, а затем выберите объем."
            )
        
        volume_map = {
            "1": "краткий",
            "2": "подробный", 
            "3": "развернутый",
            "краткий": "краткий",
            "подробный": "подробный",
            "развернутый": "развернутый"
        }
        
        volume = volume_map.get(choice.lower())
        if not volume:
            return self._send_message(
                chat_id,
                "❌ *Некорректный выбор*\n\n"
                "Пожалуйста, выберите:\n"
                "1 — Краткий конспект\n"
                "2 — Подробный конспект\n"
                "3 — Развернутый конспект"
            )
        
        # Отправляем сообщение о поиске
        search_msg = (
            f"🔍 *Ищу информацию по теме:*\n"
            f"*'{topic}'*\n\n"
            f"📊 *Выбранный объем:* {volume.capitalize()}\n"
            f"⏳ Пожалуйста, подождите 5-10 секунд..."
        )
        self._send_message(chat_id, search_msg)
        
        time.sleep(2)  # Имитация поиска
        
        # Ищем информацию
        search_info = self.searcher.search_topic(topic)
        
        # Генерируем конспект
        conspect = self.searcher.generate_conspect_from_search(topic, search_info, volume)
        
        # Обновляем статистику
        stats["conspects_created"] += 1
        self._update_stats(chat_id)
        
        # Отправляем конспект
        response = (
            f"✅ *КОНСПЕКТ ГОТОВ!*\n\n"
            f"📌 *Тема:* {topic}\n"
            f"📊 *Объем:* {volume.capitalize()} ({self._get_page_count(volume)})\n"
            f"🔍 *Поисковых запросов:* {stats['web_search_counter']}\n"
            f"⏱ *Время генерации:* {random.randint(5, 15)} секунд\n\n"
            f"━━━━━━━━━━━━━━━━━━━━\n\n"
            f"{conspect}\n\n"
            f"━━━━━━━━━━━━━━━━━━━━\n\n"
            f"💾 *Сохраните этот конспект для изучения*\n\n"
            f"🔄 *Хотите другой объем по этой теме?*\n"
            f"Просто выберите: 1, 2 или 3\n\n"
            f"🎯 *Новая тема? Просто отправьте её текстом!*"
        )
        
        return self._send_message(chat_id, response)
    
    def _get_page_count(self, volume):
        """Возвращает количество страниц для объема"""
        page_map = {
            "краткий": "0.5-1 страница А4",
            "подробный": "1-2 страницы А4", 
            "развернутый": "3-4 страницы А4"
        }
        return page_map.get(volume, "1 страница А4")
    
    def _update_stats(self, chat_id):
        """Обновление статистики"""
        user_key = str(chat_id)
        today = datetime.now().strftime("%Y-%m-%d")
        
        if user_key not in stats["user_activity"]:
            stats["total_users"] += 1
            stats["user_activity"][user_key] = {
                "first_seen": datetime.now(),
                "last_seen": datetime.now(),
                "message_count": 0
            }
        
        stats["user_activity"][user_key]["last_seen"] = datetime.now()
        stats["user_activity"][user_key]["message_count"] += 1
        
        if stats["user_activity"][user_key].get("last_active_date") != today:
            stats["active_today"] += 1
            stats["user_activity"][user_key]["last_active_date"] = today
        
        stats["total_messages"] += 1
    
    def _send_message(self, chat_id, text):
        """Отправка сообщения через Telegram Bot API"""
        import requests
        
        url = f"{self.bot_url}/sendMessage"
        payload = {
            "chat_id": chat_id,
            "text": text,
            "parse_mode": "Markdown",
            "disable_web_page_preview": True
        }
        
        try:
            response = requests.post(url, json=payload, timeout=10)
            response.raise_for_status()
            logger.info(f"Сообщение отправлено в чат {chat_id}")
            return response.json()
        except Exception as e:
            logger.error(f"Ошибка отправки сообщения: {e}")
            return None

class BotServer(BaseHTTPRequestHandler):
    """HTTP сервер для обработки вебхуков и веб-сайта"""
    
    def _set_headers(self, content_type='text/html'):
        self.send_response(200)
        self.send_header('Content-type', content_type)
        self.send_header('Access-Control-Allow-Origin', '*')
        self.end_headers()
    
    def do_GET(self):
        parsed_path = urlparse(self.path)
        path = parsed_path.path
        
        logger.info(f"GET запрос: {path}")
        
        if path == '/':
            self._serve_main_page()
        elif path == '/health':
            self._serve_health_check()
        elif path == '/stats.json':
            self._serve_stats_json()
        elif path == '/setup-webhook':
            self._setup_webhook_page()
        else:
            self.send_error(404, "Страница не найдена")
    
    def do_POST(self):
        content_length = int(self.headers['Content-Length'])
        post_data = self.rfile.read(content_length)
        
        try:
            update = json.loads(post_data.decode('utf-8'))
            update_id = update.get('update_id', 0)
            logger.info(f"Вебхук #{update_id} получен")
            
            self._process_update(update)
            
            self._set_headers('application/json')
            self.wfile.write(json.dumps({"status": "ok"}).encode('utf-8'))
            
        except Exception as e:
            logger.error(f"Ошибка обработки вебхука: {e}")
            self.send_response(500)
            self.end_headers()
    
    def _serve_main_page(self):
        webhook_url = os.getenv("RENDER_EXTERNAL_URL", "https://konspekt-helper-bot.onrender.com") + "/webhook"
        start_time = stats["start_time"]
        
        html_content = HTML_TEMPLATE.format(
            webhook_url=webhook_url,
            start_time=start_time
        )
        
        self._set_headers()
        self.wfile.write(html_content.encode('utf-8'))
    
    def _serve_health_check(self):
        health_data = {
            "status": "healthy",
            "timestamp": datetime.now().isoformat(),
            "bot": "@Konspekt_help_bot",
            "version": "4.0.0",
            "features": ["web-search", "volume-selection", "easter-egg"],
            "stats": stats.copy()
        }
        
        self._set_headers('application/json')
        self.wfile.write(json.dumps(health_data, ensure_ascii=False, indent=2).encode('utf-8'))
    
    def _serve_stats_json(self):
        today = datetime.now().strftime("%Y-%m-%d")
        active_today = sum(
            1 for user_data in stats["user_activity"].values()
            if user_data.get("last_active_date") == today
        )
        stats["active_today"] = active_today
        
        stats_data = {
            "bot": "@Konspekt_help_bot",
            "timestamp": datetime.now().isoformat(),
            "stats": stats.copy(),
            "web_searches": stats.get("web_search_counter", 0),
            "easter_egg_found": any("план захвата польши" in str(state.get("pending_topic", "")).lower() 
                                  for state in stats.get("user_states", {}).values())
        }
        
        self._set_headers('application/json')
        self.wfile.write(json.dumps(stats_data, ensure_ascii=False, indent=2).encode('utf-8'))
    
    def _setup_webhook_page(self):
        token = os.getenv("TELEGRAM_TOKEN", "ВАШ_ТОКЕН")
        webhook_url = os.getenv("RENDER_EXTERNAL_URL", "https://konspekt-helper-bot.onrender.com") + "/webhook"
        
        html_content = f"""<!DOCTYPE html>
<html>
<head><title>Настройка вебхука</title></head>
<body>
<h1>Настройка вебхука для @Konspekt_help_bot</h1>
<p><strong>URL вебхука:</strong> {webhook_url}</p>
<p><strong>Команда для настройки:</strong></p>
<pre>curl "https://api.telegram.org/bot{token}/setWebhook?url={webhook_url}"</pre>
<p><a href="/">На главную</a></p>
</body>
</html>"""
        
        self._set_headers()
        self.wfile.write(html_content.encode('utf-8'))
    
    def _process_update(self, update):
        token = os.getenv("TELEGRAM_TOKEN")
        if not token:
            logger.error("TELEGRAM_TOKEN не установлен")
            return
        
        bot = SimpleBot(token)
        
        if "message" in update:
            message = update["message"]
            chat_id = message["chat"]["id"]
            text = message.get("text", "").strip()
            update_id = update.get('update_id', 0)
            
            if text.startswith('/'):
                if text.startswith('/start'):
                    bot.start(update_id, chat_id)
                elif text.startswith('/help'):
                    help_text = "Используйте /start для начала работы. Просто отправьте тему для конспекта!"
                    bot._send_message(chat_id, help_text)
                elif text.startswith('/id'):
                    bot._send_message(chat_id, f"🆔 Ваш Telegram ID: `{chat_id}`")
                elif text.startswith('/site'):
                    web_url = os.getenv("RENDER_EXTERNAL_URL", "https://konspekt-helper-bot.onrender.com")
                    bot._send_message(chat_id, f"🌐 Веб-панель: {web_url}")
                else:
                    bot._send_message(chat_id, "❓ Неизвестная команда. Просто отправьте тему для конспекта!")
            elif text in ['1', '2', '3', 'краткий', 'подробный', 'развернутый']:
                bot.process_volume_choice(update_id, chat_id, text)
            elif text:
                bot.process_text(update_id, chat_id, text)
    
    def log_message(self, format, *args):
        pass

def start_bot():
    token = os.getenv("TELEGRAM_TOKEN")
    if not token:
        logger.error("TELEGRAM_TOKEN не установлен!")
        logger.info("Добавьте TELEGRAM_TOKEN в переменные окружения Render")
        return
    
    bot = SimpleBot(token)
    logger.info("Бот @Konspekt_help_bot с веб-поиском готов к работе")
    logger.info("🎉 Пасхалка активирована: 'план захвата польши'")

def start_http_server():
    port = int(os.getenv("PORT", 10000))
    server_address = ('', port)
    
    httpd = HTTPServer(server_address, BotServer)
    logger.info(f"HTTP сервер запущен на порту {port}")
    logger.info(f"Веб-сайт: http://localhost:{port}")
    logger.info("Пример запроса: 'дискриминант в математике'")
    logger.info("Пасхалка: 'план захвата польши'")
    
    try:
        httpd.serve_forever()
    except KeyboardInterrupt:
        logger.info("Сервер остановлен")
    except Exception as e:
        logger.error(f"Ошибка сервера: {e}")

if __name__ == "__main__":
    logger.info("=" * 60)
    logger.info("Запуск @Konspekt_help_bot с веб-поиском и пасхалкой")
    logger.info("=" * 60)
    
    # Инициализация
    if "user_states" not in stats:
        stats["user_states"] = {}
    if "web_search_counter" not in stats:
        stats["web_search_counter"] = 0
    
    token = os.getenv("TELEGRAM_TOKEN")
    if token:
        logger.info("TELEGRAM_TOKEN найден")
    else:
        logger.warning("TELEGRAM_TOKEN не найден!")
    
    bot_thread = threading.Thread(target=start_bot, daemon=True)
    bot_thread.start()
    
    start_http_server()
